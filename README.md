# موقع "مكتبة المعارف – جرد الأدوات المدرسية"

موقع ثابت (HTML/CSS/JS) يدعم اللغة العربية والاتجاه من اليمين لليسار، مع لوحة إعدادات بسيطة عبر ملف إعدادات JavaScript لتحديث المحتوى والروابط بسهولة.

## بنية الملفات

```
madrasat-library/
├── index.html          # الصفحة الرئيسية بكامل الأقسام
└── assets/
    ├── style.css       # التنسيقات والألوان
    ├── script.js       # قراءة الإعدادات وبناء البطاقات
    └── config.js       # ملف الإعدادات الذي يتم تعديله
```

## طريقة التشغيل على Windows IIS أو أي خادم ثابت
1. انقل المجلد `madrasat-library` بالكامل إلى مجلد موقعك على IIS (أو أي خادم ملفات ثابتة).
2. تأكد أن الملف الافتراضي هو `index.html`.
3. افتح الموقع في المتصفح للتأكد من عمله.

## طريقة التعديل بدون برمجة
كل التغييرات تتم عبر الملف `assets/config.js`، وهو عبارة عن كائن JavaScript يحتوي على الإعدادات التالية:

### 1. قسم `branding`
- `siteTitle`: عنوان الموقع في الترويسة.
- `siteTagline`: الوصف القصير تحت العنوان.
- `footerNote`: نص حقوق النشر؛ استخدم `{year}` لإظهار السنة الحالية تلقائياً.
- `logoEmoji`: الأيقونة الظاهرة في الترويسة (يمكن استبدالها بصورة لاحقاً).

### 2. قسم `hero`
- `eyebrow`: نص صغير أعلى العنوان الرئيسي.
- `title`: العنوان الرئيسي في البانر.
- `subtitle`: وصف موجز أسفل العنوان.
- `primaryCtaLabel`: نص زر "فتح جرد الأدوات".
- `primaryCtaUrl`: رابط Google Sheet (يُفتح في تبويب جديد).
- `secondaryCtaLabel`: نص زر ثانوي (مثلاً "طريقة التعديل").
- `secondaryCtaTarget`: يمكن أن يكون رابطاً داخلياً (#guide) أو خارجي.

### 3. قسم `inventory`
- `title`: عنوان بطاقة قسم الجرد.
- `description`: وصف مختصر للقسم.
- `buttonLabel`: نص زر فتح ملف الجرد.
- `sheetUrl`: رابط Google Sheet الذي يُفتح عند الضغط على الزر.
- `hint`: نص توضيحي يظهر أسفل الزر.

### 4. قسم `academicStages`
مصفوفة تحتوي على بطاقات المراحل/الترمات. أضف/انسخ عنصر جديد وفق النموذج التالي:
```js
{
    id: "kg1-term1",     // معرف فريد (بدون مسافات)
    grade: "KG1",         // اسم المرحلة
    year: "2025",         // السنة الدراسية
    term: "الترم الأول",  // اسم الترم
    icon: "🧸",           // أيقونة تعبيرية (Emoji)
    assets: [             // قائمة الملفات أو الروابط
        {
            type: "pdf",          // pdf | sheet | drive | image
            title: "اسم الملف",
            description: "وصف قصير للملف",
            url: "https://..."   // رابط الملف (Drive, Sheets, PDF)
        }
    ]
}
```
- يمكن ترك المصفوفة `assets` فارغة لإظهار رسالة "لا توجد ملفات مضافة حالياً".
- لتغيير ترتيب البطاقات، عدّل ترتيب العناصر داخل المصفوفة.

### حفظ التغييرات
بعد تعديل `config.js` وحفظه:
1. أعد تحميل صفحة الموقع في المتصفح.
2. ستظهر التغييرات مباشرة دون أي خطوات إضافية.

## تخصيص الألوان/الخطوط
- الألوان الأساسية معرفة في بداية `assets/style.css` داخل جذر :
  ```css
  --color-primary, --color-secondary, ...
  ```
  يمكن تغييرها لتحديث هوية الموقع.
- الخط المستخدم هو **Cairo** من Google Fonts ويمكن استبداله بروابط أخرى.

## ملاحظات إضافية
- يدعم التصميم العرض على الهواتف والأجهزة اللوحية.
- التنبيهات للأزرار مفتوحة في تبويب جديد (`target="_blank"`) للحفاظ على الصفحة الأساسية.
- يمكن إضافة صور أو أيقونات مخصصة بدلاً من الرموز التعبيرية عند الحاجة.

> احتفظ بنسخة احتياطية من `config.js` قبل إجراء تغييرات كبيرة، تحسباً للرجوع إلى الإعدادات السابقة.
